# 城市天气查询小程序 - 新手使用教程

## 📋 简介

这是一个纯 Python + Tkinter 编写的城市天气查询 GUI 应用程序，支持：
- 输入中文城市名查询实时天气
- 可视化展示天气数据（温度、湿度、风向等）
- 一键复制天气文本到剪贴板
- 自动保存最近查询的 10 个城市历史记录
- 本地保存 API 密钥配置

## 🔧 环境要求

- **操作系统**: Windows 7/10/11、macOS、Linux
- **Python 版本**: Python 3.7 或更高版本
- **依赖库**: 
  - `tkinter`（Python 内置，无需安装）
  - `requests`（需要安装）
  - `pyperclip`（可选，用于剪贴板功能）

## 📦 依赖安装

### 1. 安装 Python

如果尚未安装 Python，请访问官网下载安装：
- 官网地址：https://www.python.org/downloads/
- **注意**：安装时务必勾选 "Add Python to PATH" 选项

### 2. 安装依赖库

打开命令行（Windows 按 `Win+R` 输入 `cmd` 回车），执行以下命令：

```bash
# 安装 requests 库（必需）
pip install requests

# 安装 pyperclip 库（可选，推荐安装）
pip install pyperclip
```

如果安装速度慢，可以使用国内镜像源：
```bash
pip install requests pyperclip -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 🔑 API 密钥申请

本程序使用**高德地图天气 API**，需要申请免费的 API 密钥（Key）。

### 申请步骤：

1. **访问高德开放平台**
   - 打开浏览器访问：https://lbs.amap.com/

2. **注册/登录账号**
   - 点击右上角"注册"按钮
   - 使用手机号注册并完成实名认证

3. **创建应用**
   - 登录后进入控制台：https://console.amap.com/dev/index
   - 点击左侧菜单「应用管理」→「我的应用」
   - 点击右上角「创建新应用」
   - 应用名称填写：天气查询
   - 应用类型选择：其他

4. **添加 Key**
   - 在创建的应用卡片中点击「添加Key」
   - Key 名称填写：天气查询Key
   - 服务平台选择：**Web服务**
   - 点击「提交」

5. **获取 Key**
   - 添加成功后，会显示一串字符（如：`a1b2c3d4e5f6...`）
   - 这就是你的 API 密钥，复制保存好

### 免费额度说明

- 个人开发者：每天 5000 次调用额度
- 对于个人使用完全足够

## 🚀 运行程序

### 方法一：直接双击运行

1. 确保已安装 Python 并配置好环境变量
2. 双击 `weather_app.py` 文件即可运行

### 方法二：命令行运行

```bash
# 进入程序所在目录
cd 你的程序目录

# 运行程序
python weather_app.py
```

## 📱 使用说明

### 首次使用

1. 运行程序后会弹出欢迎提示
2. 点击右下角的「⚙️ API设置」按钮
3. 粘贴你申请的高德地图 API 密钥
4. 点击「保存」

### 查询天气

1. 在顶部输入框输入城市名称（如：北京、上海、广州）
2. 点击「查询」按钮或按回车键
3. 天气信息会显示在中间面板

### 快捷功能

- **历史记录**：点击历史记录中的城市按钮可快速查询
- **复制天气**：点击「📋 复制天气」按钮可复制天气文本
- **清空历史**：点击「🗑 清空历史」按钮可清除所有历史记录

### 天气图标说明

程序使用 emoji 图标直观展示天气状况：
- ☀️ 晴
- ⛅ 多云
- ☁️ 阴
- 🌦️ 小雨
- 🌧️ 中雨/大雨
- ⛈️ 暴雨/雷阵雨
- 🌨️ 雪
- 🌫️ 雾
- 😷 霾

## ⚠️ 常见问题

### 1. 提示 "请先配置 API 密钥"

**原因**：首次运行尚未配置高德地图 API 密钥

**解决**：
1. 点击「⚙️ API设置」按钮
2. 按照上方「API 密钥申请」步骤获取密钥
3. 粘贴密钥并保存

### 2. 提示 "未找到城市"

**原因**：输入的城市名称不正确或不在支持范围内

**解决**：
- 尝试输入完整城市名（如："北京市" 而不是 "北京"）
- 高德 API 支持省、市、区县级别查询
- 部分偏远地区可能不支持

### 3. 提示 "网络连接失败"

**原因**：网络连接问题或防火墙限制

**解决**：
- 检查网络连接是否正常
- 关闭 VPN 或代理软件后重试
- 检查防火墙是否阻止了 Python 的网络访问

### 4. 提示 "API 密钥无效"

**原因**：密钥填写错误或服务平台选择错误

**解决**：
1. 重新检查密钥是否正确复制
2. 登录高德控制台确认 Key 的服务平台是「Web服务」
3. 如仍有问题，删除旧 Key 重新创建

### 5. 界面显示异常（字体过小/过大）

**原因**：高分辨率屏幕适配问题

**解决**：
- Windows：右键程序 → 属性 → 兼容性 → 更改高 DPI 设置 → 勾选「替代高 DPI 缩放行为」
- 程序已内置 DPI 感知代码，大部分情况可自动适配

## 📁 配置文件位置

程序会在以下位置创建配置文件：

- **Windows**: `C:\Users\你的用户名\.weather_app\`
- **macOS**: `/Users/你的用户名/.weather_app/`
- **Linux**: `/home/你的用户名/.weather_app/`

包含两个文件：
- `config.json`：存储 API 密钥
- `history.json`：存储查询历史

如需完全重置程序，删除整个 `.weather_app` 文件夹即可。

## 🛠️ 卸载程序

本程序为绿色软件，无需卸载：
1. 删除 `weather_app.py` 文件
2. 删除配置文件夹 `.weather_app`（可选）

## 📞 技术支持

如有问题，请检查：
1. Python 版本是否为 3.7+
2. 依赖库是否已正确安装
3. API 密钥是否配置正确
4. 网络连接是否正常

## 📄 开源协议

本程序为学习示例代码，可自由使用和修改。

---

**祝你使用愉快！** ☀️
