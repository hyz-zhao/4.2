# 城市天气查询小程序 - 使用教程

## 一、依赖安装

### 1.1 安装 Python

确保你的电脑已经安装了 Python 3.6 或更高版本。你可以在终端（Windows 命令提示符或 PowerShell）中输入以下命令检查：

```bash
python --version
```

如果没有安装，请访问 [Python 官网](https://www.python.org/downloads/) 下载并安装。

### 1.2 安装 requests 库

本程序仅依赖 `requests` 库（用于网络请求），其他库均为 Python 内置。

在终端中运行以下命令安装：

```bash
pip install requests
```

如果安装速度较慢，可以使用国内镜像源：

```bash
pip install requests -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 二、申请高德地图天气 API 密钥

### 2.1 注册账号

1. 访问 [高德开放平台](https://lbs.amap.com/)
2. 点击右上角的「注册」按钮
3. 按照提示完成注册流程

### 2.2 创建应用并获取 Key

1. 登录后，进入「控制台」
2. 点击左侧菜单的「应用管理」→「我的应用」
3. 点击「创建新应用」
4. 填写应用名称（如：天气查询），选择应用类型（如：Web端(JS API)）
5. 创建后，点击「添加 Key」
6. 填写信息：
   - Key 名称：随意填写（如：天气查询）
   - 服务平台：选择「Web服务」
7. 点击「提交」，你将获得一个 API Key（一串字符）

**重要说明**：
- 免费额度充足，个人使用完全足够
- 请妥善保管你的 Key，不要泄露给他人

## 三、运行程序

### 3.1 启动程序

在终端中进入程序所在目录，然后运行：

```bash
python weather_app.py
```

或者直接双击 `weather_app.py` 文件（如果已关联 Python）。

### 3.2 配置 API 密钥

1. 程序启动后，点击右下角的「设置密钥」按钮
2. 在弹出的窗口中输入你申请到的高德 API Key
3. 点击「保存」

### 3.3 查询天气

1. 在顶部输入框中输入城市名（支持省、市、简称）
2. 点击「查询」按钮或按回车键
3. 天气数据将显示在中间区域

### 3.4 其他功能

- **历史记录**：最近查询的 10 个城市会显示在历史记录栏，点击可快速查询
- **复制天气**：点击「复制天气」按钮，可将当前天气信息复制到剪贴板
- **清空历史**：点击「清空历史」按钮，可清空所有历史查询记录

## 四、常见问题

### Q: 提示「API密钥无效，请重新配置」
A: 请检查你的 API Key 是否正确，确保选择了「Web服务」类型。

### Q: 提示「未找到该城市的天气数据」
A: 请检查城市名称是否正确，尝试使用标准城市名（如「北京」而不是「帝都」）。

### Q: 提示「网络请求失败」
A: 请检查你的网络连接是否正常。

## 五、配置文件说明

程序的配置文件保存在：
- Windows: `C:\Users\你的用户名\.weather_app\`
- macOS/Linux: `~/.weather_app/`

包含两个文件：
- `config.json`：保存 API 密钥
- `history.json`：保存查询历史记录

## 六、技术说明

- **GUI框架**：Tkinter（Python内置）
- **网络请求**：requests
- **数据存储**：JSON文件
- **天气数据源**：高德地图天气API

---

祝你使用愉快！
